{
    "version": 2,
    "builds": [
        {
            "src": "./api/genui.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api"
        }
    ]
}